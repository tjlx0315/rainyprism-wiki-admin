# macOS / external-drive metadata
.DS_Store
._*

# Local code backups
index.single-file.backup.html
index_备份.html
js/*.backup.js
backups/

# Exported local world data
雨棱镜-世界观备份-*.json

# Local environment and secrets
.env
.env.*
!.env.example

# Editor files
.vscode/
.idea/
*.swp
*~

# Local-only worldbuilding editor
# These files stay on this Mac / external drive and are never published.
/index.html
/css/
/js/
/安全备份/
/启动百科前端.command
/停止百科前端.command

# Local duplicate / working copy; the only published site lives in /wiki.
/百科前端/
